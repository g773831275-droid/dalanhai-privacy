# 大蓝海 · 上架待办清单

> 从现在到 App Store 上线，按顺序执行。每完成一步打勾，遇到卡点告诉我。
> 预计总耗时：你操作约 2-4 小时 + 苹果审核 24-48 小时。

---

## 已完成（不用再动）

- [x] PRD 文档（V1.2 免费化版）
- [x] H5 App 原型 `index.html`（打卡/证书/注销/客服二维码，广告补签已隐藏）
- [x] Capacitor 工程配置（`package.json` / `capacitor.config.json`）
- [x] App 图标 `icon-1024.png`
- [x] 隐私政策 `privacy-policy.html`（已填开发者姓名、客服微信、邮箱）
- [x] 5 张上架截图 `shots/shot1-5.png`
- [x] 客服微信二维码 `wechat-qr.jpg`

---

## 第 1 步：注册 Bundle ID（你做 · 浏览器 · 5 分钟）

**目的**：给 App 申请唯一标识，打包签名要用。

1. 打开 https://developer.apple.com/account/resources/identifiers/list （用你的开发者账号登录）
2. 点左上角「**+**」
3. 选「App IDs」→ Continue → 选「App」→ Continue
4. 填：
   - Description：`大蓝海`
   - Bundle ID：选「**Explicit**」，输入 `com.kaitengguan.dalanhai`
5. Capabilities 默认不动 → Continue → Register

**完成标志**：Identifiers 列表里出现 `com.kaitengguan.dalanhai`
**注意**：必须和 `capacitor.config.json` 里的 appId 完全一致（已配好，别打错）

---

## 第 2 步：托管隐私政策到 GitHub Pages（你做 · 浏览器 · 5 分钟）

**目的**：拿到一个能正常渲染的 https 链接，填进 App Store Connect。

1. 登录 https://github.com （没账号注册一个，免费不用实名）
2. 点右上「+」→「New repository」
   - 名字填 `dalanhai-privacy`
   - 选「**Public**」
   - 不勾 "Add a README"
   - Create
3. 仓库页面「Add file」→「Upload files」→ 拖入本地文件：
   ```
   C:\Users\gktvi\WorkBuddy\2026-07-24-00-36-22\dalanhai\privacy-policy.html
   ```
   - Commit message 填 `add privacy policy` → Commit changes
4. 仓库「**Settings**」→ 左侧「**Pages**」
   - Source：Deploy from a branch
   - Branch：选 `main`，文件夹 `/(root)` → Save
5. 等 1-2 分钟，页面顶部显示链接：
   ```
   https://你的GitHub用户名.github.io/dalanhai-privacy/privacy-policy.html
   ```

**完成标志**：浏览器打开这个链接，能看到正常渲染的隐私政策网页（标题、段落、列表，不是源码）
**注意**：把"你的GitHub用户名"换成你实际的用户名

> 第 1、2 步可以同时做，互不影响。

---

## 第 3 步：搞到一台 Mac（你做 · 1-3 小时）

**目的**：iOS 打包必须在 macOS + Xcode 上做，绕不开。

**方案 A · 租云 Mac（推荐）**
- MacinCloud：https://www.macincloud.com 按小时租（约 $1-2/小时）
- 租前问客服是否预装 Xcode（预装能省 1-2 小时下载时间）
- 租 4-6 小时足够打一次包

**方案 B · 借朋友的 Mac**
- 一次性用 2-3 小时

**Mac 上要装的东西**（云 Mac 没预装的话）：
```bash
brew install node          # Node.js
# Xcode 从 Mac App Store 下载（约 12GB，最耗时）
sudo gem install cocoapods # Capacitor iOS 依赖
```

**完成标志**：Mac 上能跑 `node -v`、`xcodebuild -version`、`pod --version` 三个命令都有输出

---

## 第 4 步：在 Mac 上打包出 .ipa（你在 Mac 操作 · 30-60 分钟）

**目的**：把 H5 包成 iOS App，生成可上传的安装包。

1. 把本地 `dalanhai/` 整个目录传到 Mac（GitHub 私有仓 / 网盘 / scp 都行）
   - 注意：`node_modules/` 不用传，Mac 上重新装
2. 在 Mac 上打开终端，进入目录：
   ```bash
   cd dalanhai
   npm install              # 装 Capacitor
   npx cap add ios          # 生成 iOS 工程
   npx cap sync ios         # 同步 Web 资源
   npx cap open ios         # 用 Xcode 打开
   ```
3. Xcode 里配置签名：
   - 左侧选 `App` → 「Signing & Capabilities」标签
   - Team 选你的开发者账号
   - Bundle Identifier 确认是 `com.kaitengguan.dalanhai`
4. 打包上传：
   - Xcode 顶部设备选「**Any iOS Device (arm64)**」
   - 菜单「Product」→「**Archive**」（等归档完成）
   - 弹出 Organizer →「**Distribute App**」→「App Store Connect」→「Upload」

**完成标志**：Xcode 显示"Upload Successful"，App Store Connect 里能看到这个构建版本
**卡点求助**：签名失败、Archive 报错，把错误信息发我

---

## 第 5 步：App Store Connect 填元数据（你做 · 浏览器 · 30 分钟）

**目的**：把 App 信息、截图、隐私政策填全，让审核员有东西看。

1. 打开 https://appstoreconnect.apple.com →「我的 App」→「+」→「新建 App」
   - 平台：iOS
   - 名称：大蓝海
   - 主语言：简体中文
   - Bundle ID：选 `com.kaitengguan.dalanhai`
   - SKU：`dalanhai`
2. 填「App 信息」：
   - 副标题：`男性自律打卡岛屿 · 21天养成好习惯`
   - 分类：生活方式（Lifestyle）
3. 填「版本信息」（选刚上传的构建版本）：
   - 描述、推广文本、关键词：照《上架打包指南.md》第三章复制
   - 截图：上传 `shots/shot1-5.png`（6.7 寸那组）
   - App 图标：已随构建上传，不用单独填
4. 「App 隐私」：
   - 勾选收集的数据类型（用户内容、标识符）
   - 隐私政策 URL：填第 2 步拿到的 GitHub Pages 链接
5. 「年龄分级」：勾选无成人内容/赌博/暴力 → 结果应是 **12+**
6. 「App 隐私」里「账号删除」：确认 App 内有注销入口（已有）

**完成标志**：所有带红点的项目都填完，没有缺项

---

## 第 6 步：提交审核（你做 · 点几下 · 1 分钟）

1. App Store Connect → 该 App → 「提交审核」
2. 一路确认，选审核加急（可选，一般不用）

**完成标志**：状态变成「正在审核」

---

## 第 7 步：等审核结果（苹果做 · 24-48 小时）

- 状态会从「正在审核」→「待开发者发布」或「被拒」
- **被拒**：看 Resolution Center 的拒因，常见原因和对策在《上架打包指南.md》第六章
- 被拒后：修改 → 重新 Archive 上传 → 重新提交

**完成标志**：状态变成「可供销售」→ App 上线 🎉

---

## 你现在立刻能做的

**今天就能并行推进的**（不需要 Mac）：
- ✅ 第 1 步：注册 Bundle ID（5 分钟）
- ✅ 第 2 步：GitHub Pages 托管隐私政策（5 分钟）

**需要 Mac 才能做的**：
- ⏳ 第 3-4 步：搞 Mac + 打包

**建议**：先把第 1、2 步做了，同时去租云 Mac。Mac 到手后照第 4 步打包。打包和填元数据（第 5 步）可以并行。

---

## 每一步完成后

把进度告诉我，遇到任何卡点（注册失败、Pages 打不开、Xcode 报错、被拒）把截图或错误信息发我，我帮你判断下一步。
